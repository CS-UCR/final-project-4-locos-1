import React from 'react'
import { View, Text } from 'react-native'

const WorkSpaceMap = (props) => {
    return(
        <View>
            <Text> WorkSpaceMap</Text>
        </View>

    )
}

export default WorkSpaceMap